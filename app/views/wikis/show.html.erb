<div class="">
  <h1><%= @wiki.title %></h1>
</div>
<div>
  <p>
    <%= @body.html_safe %>
  </p>
</div>
<div class="btn-container">
  <div class="col-md-3 index-btn">
    <%= link_to "Delete", wiki_path(@wiki), method: :delete, data: { confirm: "Are you sure you want to delete this wiki?" } %>
  </div>
  <div class="col-md-3 index-btn">
    <%= link_to "Edit", edit_wiki_path(@wiki) %>
  </div>
</div>
